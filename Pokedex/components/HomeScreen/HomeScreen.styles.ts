import { StyleSheet } from "react-native"

const home_screem_styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
    text: {
        fontSize: 30,
        marginBottom: 30,
    },
    button: {
        backgroundColor: 'blue',
        borderRadius: 15,
        textAlign: 'center',
        padding:20,
    }
})

export default home_screem_styles