import { StyleSheet } from "react-native"

const view_styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    text: {
        fontSize: 30,
        fontWeight: 'bold',
    },
    image: {
        flex: 1,
    }
})

export default view_styles