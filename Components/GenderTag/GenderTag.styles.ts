import {StyleSheet} from "react-native";

export const genderTagStyles = StyleSheet.create({
    genderTag: {
        textTransform: "capitalize",
        flexDirection: "row",
        width: 90,
        gap: 5,
        backgroundColor: "#f0efed",
        padding: 5,
        alignItems: "center",
        justifyContent: "center",
        borderRadius: 100
    },
})